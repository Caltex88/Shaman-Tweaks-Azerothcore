Patched 3.3.5a DBC for the server DataDir.

  Spell.dbc              vanilla + Lava Burst ranks 910001-910007 + instant Ghost Wolf
  SkillLineAbility.dbc   vanilla + shaman spellbook rows for those ranks

Copy these into the worldserver DataDir/dbc folder (or run
apps/install-server-data.sh after make install). They replace the stock
files of the same name.
